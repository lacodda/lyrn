# What this repository was generated from, and which revision of the line's
# standard it carries. `lyrn doctor` and `lyrn upgrade` read this file.
[project]
name = "{{ name }}"
form = "{{ form }}"
accent = "{{ accent }}"

[template]
version = "{{ lyrn_version }}"
standard = "{{ standard }}"
